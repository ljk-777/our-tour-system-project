# 第十三周项目周报

## 一、本周项目整体进展

本周（5月31日—6月6日）项目进入收尾冲刺阶段，全员发力，主线工作多线并进：戴鸿完成 Globe 流星弹幕特效完整版（含性能优化）和城市相机定位精确化；缑文轩大幅升级群组协作中枢，GroupDetail 页面全面重写；李佳坤完成景点图片本地化最终版（128张实景图）并工具化图片管理流程；万子豪完成 Admin 后台数据更新，并协助群组功能后端联调。全组四人本周均有实质性代码产出。

---

## 二、本周各模块进展

### 1. Globe 流星弹幕系统（`src/frontend/src/components/GlobeOverlay.jsx`、`Earth3D.jsx`）
- 城市聚焦后屏幕竖向1/3定位实现（戴鸿）
- 流星弹幕 DanmuLayer 初版→密集版→性能优化版，共7次提交迭代
- 彻底移除 shadowBlur，改用双层渐变线+fillRect，帧率大幅提升
- Globe 旋转停顿修复：brake 衰减加速 + ease-in-out-cubic
- 相机旋转数学公式彻底重写：正确3D向量推导，处理垂直相机边界情况

### 2. 群组协作功能升级（`src/backend/src/routes/groups.js`、`src/frontend/src/pages/GroupDetail.jsx`）
- GroupDetail 页面大规模重写（缑文轩）：地图路线、AI 行程助手、偏好管理、冲突协调
- `groupRepository.js` 大幅扩展：投票、行程、偏好、消息等多项数据库操作
- `index.css` 新增 `.glass-input`、`.glass-btn`、`.glass-panel` 等组件样式类
- 群组协作中枢升级：群组码加入、AI @小迹 聊天、天气建议

### 3. 群组四项新功能（本周新增）
- 共享景点到群（戴鸿）：SpotDetail 页增加📤分享按钮，群聊展示景点卡片
- 群内 Dijkstra 路线规划（戴鸿）：RouteTab 新增绿色 Dijkstra 按钮，调用本地算法返回最优景点顺序
- 退出群组：已在后端 `POST /:id/leave` 和前端按钮上完整实现
- 目的地投票：PollCard 组件 + 后端完整投票路由已上线

### 4. 景点图片本地化（`src/frontend/public/images/spots/`、`src/backend/src/data/spots.js`）
- 景点图片本地化最终版（李佳坤）：128张实景图下载入库，补全 BrandIcon 品牌回退逻辑
- 新增 `scripts/` 目录：`add-image-urls.js`、`add-local-paths.js`、`download-spot-images.js`

### 5. Admin 后台更新（`src/frontend/src/pages/Admin.jsx`）
- Admin 后台数据更新，完善管理员操作流程（万子豪）
- 协助群组功能后端接口联调，确保前后端数据格式一致

---

## 三、本周每位同学工作进展

### 1. 戴鸿（前端·Globe 动效 + 群组功能）
- Globe 弹幕系统从初版到性能优化版，共7次迭代，彻底解决帧率卡顿
- 城市定位精度修复（二次镜头跳动消除、相机旋转公式重写）
- 新增共享景点到群功能（SpotDetail 📤 按钮 + GroupDetail SpotCard）
- 新增群内 Dijkstra 一键路线规划（RouteTab 绿色按钮 + 后端 multiPointPath 接入）

### 2. 李佳坤（后端·数据与图片）
- 景点图片本地化最终版：128张实景图 + BrandIcon 回退 + 三个管理脚本工具化
- 优化景点数据 `spots.js`，补全 localImagePath 字段覆盖率
- 协助缑文轩完善群组数据库字段设计，审查后端路由安全性

### 3. 万子豪（前端·Admin 后台）
- Admin 后台数据更新：补全景点分类标签、坐标数据及路网权重
- 协助群组功能后端联调：排查前后端数据格式差异，修复接口类型转换问题
- 完善系统整体测试，走通注册→登录→景点浏览→群组→路线规划完整用户路径

### 4. 缑文轩（前端·社区与群组）
- 群组协作中枢大规模升级：GroupDetail 全面重写、AI @小迹 聊天、天气建议、行程偏好
- 后端群组路由扩展：投票、成员管理、消息类型扩充
- 负责 PR #47/#48/#49/#50 合并管理
- 补充群组模块单元测试，覆盖投票边界、成员权限、消息类型异常等场景

---

## 四、当前存在的问题

1. **七周连续滞后：KMP 检索可见化** — KMP 算法在前端页面上始终不可见，这是答辩前最高优先级的遗留缺口。现有日记搜索未展示 KMP 高亮，算法价值无法被评委感知。

2. **对抗基础设施入库（连续七周滞后）** — `adversarial/`、`mcp/` 从未进入主分支，review-agent 从未真实运行。

3. **Globe 弹幕卡顿已修复，但城市定位需最终验证** — 需多角度点击城市验证旋转正确性，确认不同纬度城市的屏幕位置正常。

4. **答辩演示链路未完全固化** — Dijkstra（路线规划页）、KMP（日记/景点搜索高亮）、TopK（美食推荐）、Trie（搜索联想）四条算法演示路径尚未全部走通截图留存。

---

## 五、下周计划

1. **【最高优先级】KMP 检索前端可见化**：实现日记或景点搜索关键词高亮展示，让 KMP 算法在页面上真正可见。
2. **Globe 最终稳定性验证**：多角度点击城市验证旋转正确性，确认弹幕无卡顿。
3. **系统整体功能回归测试**：走一遍完整用户路径，确认无功能性 bug。
4. **Trie 前缀联想前端联动**：景点搜索框接入 Trie 自动补全，补全算法在前端的可见性。
5. **代码质量收尾**：补全关键模块注释与 JSDoc，整理 README 部署说明。
